const User = require('./User'); // Imports the User model
const Thought = require('./Thought'); //Imports the Thought model

module.exports = { User, Thought } //Exports the use of the User and Thought models